(ns clj.core-test
  (:require [clojure.test :refer :all]
            [clj.main :refer :all]
            [clj.macro :refer :all]
            ))

(deftest test-1
  (testing "FIXME, I fail."
    (is (= 1 2))))
(deftest test-2
  (testing "..."
    (is (= 1 1))))
